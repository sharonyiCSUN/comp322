#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <semaphore.h>
#include <pthread.h>

int eat(){
int usleep();
int rand();
  return 0;
}

int think(){

int usleep();
return 0;
}

int main(int argc, char &argv[]){

semaphore chopstick[5];

do{
  wait(chopstick[i]);
  wait(chopstick[(i + 1) % i]);

  signal(chopstick[i]);
  signal(chopstick[(i + 1) % i]);
}while(true);



return 0;
}

